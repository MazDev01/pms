-- ── สั่งล้างแคชโครงสร้างฐานข้อมูลของ PostgREST ────────────────────────────────
--
-- ทำไมต้องมี: PostgREST (ตัวที่รับคำขอจากแอปแล้วคุยกับฐานข้อมูล) จำ "หน้าตาของฟังก์ชัน/ตาราง"
--   ไว้ในแคชของตัวเอง ปกติมีตัวคอยสั่งล้างให้อัตโนมัติเมื่อโครงสร้างเปลี่ยน
--   แต่ถ้าจังหวะไม่ดี (เช่น เพิ่งลบฟังก์ชันรุ่นซ้ำทิ้งใน 0162) แคชอาจยังจำของเก่าอยู่
--   อาการที่เห็น: ตอบ PGRST202 "ไม่พบฟังก์ชัน" หรือ PGRST203 "เลือกฟังก์ชันไม่ถูก"
--   ทั้งที่ในฐานข้อมูลถูกต้องแล้ว
--
-- คำสั่งนี้ไม่แก้ข้อมูลหรือโครงสร้างใด ๆ — แค่บอกให้ไปอ่านโครงสร้างใหม่รอบหนึ่ง
notify pgrst, 'reload schema';
