-- Benjamin PMS — สแนปช็อต % VAT ติดใบตอนสร้าง (จากผลตรวจสอบตรรกะระบบ 31 ก.ค. 69)
--
-- เดิม: ยอด VAT คำนวณสดจาก hq_policy.vat ปัจจุบันทุกครั้งที่เปิดดู/พิมพ์ใบ ไม่เคยตรึงไว้กับใบ
--   ต่างจากข้อมูลหัวกระดาษอื่น (issuer) ที่สแนปช็อตไว้ตั้งแต่สร้าง — ถ้า HQ เปลี่ยนอัตรา VAT ทีหลัง
--   พิมพ์ใบเก่าซ้ำจะได้ยอดไม่ตรงกับที่เสนอราคาลูกค้าจริงตอนนั้น
--
-- ใบเก่าที่ไม่มีค่านี้ (NULL) ยัง fallback ไปใช้ hq_policy.vat ปัจจุบันเหมือนเดิม (ไม่กระทบข้อมูลเก่า)

alter table quotations add column if not exists vat_percent numeric;
