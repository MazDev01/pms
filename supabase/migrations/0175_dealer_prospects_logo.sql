-- Benjamin PMS — ลูกค้าเป้าหมาย (HQ): รูปประจำตัว (บอสสั่ง 14 ก.ย. 69: "ใส่รูปไปด้วย" · "แบบหน้า dealer" · "มีเพิ่มรูปตั้งแต่ในนี้")
--
-- แบบเดียวกับ leads.logo ของตัวแทน: หน้าจอย่อรูปเหลือ 256px แล้วเก็บเป็นข้อความ (data URL) ในแถวเลย
--   ไม่แยกไปคลังไฟล์ — รูปเล็ก ใช้ขึ้นหัวแผงแทนตัวย่อชื่อ ไม่ต้องจัดการสิทธิ์คลังไฟล์เพิ่ม
--
-- ด่านที่ฐานข้อมูล (ยิง API ตรงก็โดน):
--   • ต้องเป็นรูปจริง (ขึ้นต้น data:image/) — กันยัดลิงก์ภายนอก/ข้อความอื่นมาแสดงในหน้าจอ HQ
--   • ยาวไม่เกิน 400,000 ตัวอักษร (รูป 256px ที่ย่อแล้วราว 20–60 KB) — กันยัดไฟล์ใหญ่จนตารางอืด
alter table public.dealer_prospects
  add column if not exists logo text
    check (logo is null or (logo like 'data:image/%' and char_length(logo) <= 400000));

comment on column public.dealer_prospects.logo is
  'รูปประจำตัว (data URL ย่อ 256px จากหน้าจอ) — ไม่บังคับ · ว่าง = แสดงตัวย่อชื่อ';
