-- ── ลบ network_customer_summary() รุ่นไม่มีพารามิเตอร์ ที่ค้างอยู่คู่กับรุ่นใหม่ ──────────
--
-- ความผิดพลาดของใบ 0155: ใช้ create or replace กับ "ลายเซ็นใหม่" (3 พารามิเตอร์)
--   ซึ่งไม่ได้แทนที่ของเดิม แต่กลายเป็นฟังก์ชันคนละตัวที่ชื่อซ้ำกัน
--   → PostgREST เจอสองตัวที่เรียกด้วยศูนย์พารามิเตอร์ได้เหมือนกัน เลือกไม่ถูก ตอบ PGRST203
--   → การ์ด "ลูกค้าทั้งเครือ" บนแดชบอร์ดขึ้นค่าไม่ได้เลย (ยืนยันจากการเรียกจริงหลัง push)
--
-- บทเรียนเดิมที่เคยเขียนเตือนไว้แล้วในใบ 0113/0154 แต่ยังพลาดซ้ำ:
--   "เพิ่มพารามิเตอร์ให้ฟังก์ชันเดิม" = ต้องลบรุ่นเก่าทิ้งเสมอ ไม่ใช่แค่สร้างรุ่นใหม่
--   เพราะ Postgres ถือว่าจำนวน/ชนิดพารามิเตอร์ต่างกัน = คนละฟังก์ชัน
--
-- หลังลบแล้ว การเรียกแบบไม่ส่งพารามิเตอร์ยังทำงานเหมือนเดิมทุกประการ
--   (รุ่นใหม่ตั้ง default null ไว้ครบทุกตัว = เรียกเปล่า ๆ ได้ ผลลัพธ์เท่าของเดิม)
drop function if exists public.network_customer_summary();
