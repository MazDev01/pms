-- Benjamin PMS — "งานมาตรฐาน" ของแต่ละขั้นในเส้นทางการขาย ให้ HQ แก้ได้ (สั่งโดยผู้บริหาร 13 ส.ค. 69)
--
-- เดิม: ชุดงาน (ติดต่อครั้งแรก · เก็บข้อมูลลูกค้า · จัดทำใบเสนอราคา …) ฝังตายอยู่ในโค้ด
--       (LEAD_TASK_TEMPLATE ใน mock.ts) หน้า HQ ตั้งค่าจึงแสดงได้อย่างเดียว แก้ไม่ได้เลย
--       แต่ชุดงานนี้คือ "ตัวขับความคืบหน้า" จริงของลีดทุกใบ — เช็กงานแล้วสถานะเลื่อนขั้นเอง
--
-- เก็บไว้ที่ตารางเดิม (hq_sales_journey แถวเดียว id=1) ที่มี "เหตุผลปิดไม่สำเร็จ" อยู่แล้ว
-- เพราะเป็นเรื่องเดียวกัน (กติกาเส้นทางการขายของทั้งเครือ) และได้ RLS + Realtime ชุดเดิมทันที:
--   · อ่าน: ทุกคนที่ล็อกอิน (ตัวแทนต้องเห็นงานที่ HQ ตั้ง)  · เขียน: can_write_master() เท่านั้น
--   · Realtime: HQ กดบันทึก → หน้าตัวแทนเปลี่ยนตามโดยไม่ต้องรีเฟรช
--
-- default null = "ยังไม่เคยตั้ง" → แอปใช้ชุดเริ่มต้นในโค้ด (ไม่ต้อง seed ค่าเริ่มต้นซ้ำที่ DB
-- ซึ่งจะกลายเป็นสองแหล่งที่หลุดจากกันเมื่อค่าเริ่มต้นในโค้ดเปลี่ยน)
--
-- รูปแบบข้อมูล: [{ "key": "contact", "label": "ติดต่อครั้งแรก", "stage": "WAITING" }, ...]
--   key   = รหัสงาน (ผูกกับ tasks ที่บันทึกไว้ในลีดแต่ละใบ — เปลี่ยนแล้วงานเก่าจะไม่แมตช์)
--   stage = ขั้นที่งานนี้พาลีดไปถึง (ค่าเดียวกับ enum lead_status)
--   งาน key='close' (ปิดการขาย) ต้องมีเสมอ — แอป normalize เติมกลับให้ถ้าหาย

alter table hq_sales_journey add column if not exists tasks jsonb;

comment on column hq_sales_journey.tasks is
  'งานมาตรฐานรายขั้นที่ HQ ตั้ง (null = ใช้ชุดเริ่มต้นในโค้ด) — [{key,label,stage}]';
