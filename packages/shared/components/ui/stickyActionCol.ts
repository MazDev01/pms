import type { CSSProperties } from "react";

// ── ตรึงคอลัมน์ปุ่มไว้ขวาสุดของตาราง ─────────────────────────────────────────────
//
// ทำไมต้องมี (บอสทัก 26 ส.ค. 69): ตารางของ HQ หลายตัวกว้างเกินพื้นที่ที่จอโน้ตบุ๊ก 1280
//   มีอยู่จริง (เช่น ตารางตัวแทน 1120px แต่พื้นที่ ~968px) คอลัมน์ปุ่มจึงหลุดออกนอกกรอบ
//   ต้องเลื่อนแนวนอนก่อนถึงจะกดได้ · ผู้ใช้ที่ไม่ทันสังเกตจะเห็นปุ่มโดนตัดครึ่งแล้วคิดว่าระบบพัง
//
// ทำไมไม่ใช้วิธี "หดคอลัมน์ให้พอดีจอ": เคยทำแล้วได้เนื้อหาโดนเฉือน ("C..." แทน "CNX")
//   ตรึงปุ่มไว้แทน = ปุ่มกดได้ทุกความกว้างจอ โดยไม่ต้องตัดข้อมูลคอลัมน์ไหนทิ้งเลย
//
// ⚠️ พื้นหลังต้องทึบเสมอ ไม่งั้นเนื้อหาคอลัมน์อื่นจะเลื่อนมาซ้อนใต้ปุ่มจนอ่านปนกัน
export const ตรึงคอลัมน์ปุ่ม = (หัวตาราง = false): CSSProperties => ({
  position: "sticky",
  right: 0,
  zIndex: 2,
  background: หัวตาราง ? "#f8fafc" : "#fff",
  // เส้นเงาจาง ๆ ด้านซ้าย บอกว่าแถบนี้ลอยอยู่ ไม่ใช่ตารางเพี้ยน
  boxShadow: "-8px 0 8px -8px rgba(15,23,42,.18)",
});
