# dependencies (ทุกชั้น — โมโนเรโป: root + apps/* + packages/*)
node_modules/

# build output / cache (ทุกชั้น)
.next/
.turbo/
out/
build/
*.tsbuildinfo
next-env.d.ts

# env
.env*
!.env.example

# misc
.DS_Store
*.zip

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# debug/scratch scripts
_m.mjs
.scratch-audit/

# Claude Code personal local settings + เครื่องมือส่วนตัว
.claude/settings.local.json
.claude/hooks/

# เครื่องมือ/ข้อมูลส่วนตัว (ไม่ผูกกับโปรเจกต์)
.vscode/
note/

# ผลลัพธ์/ภาพหน้าจอจาก Playwright MCP — เป็นไฟล์ชั่วคราวขนาดใหญ่ (เคยถูก commit ไป 192 ไฟล์ ~65MB)
# และเนื้อหามีข้อมูลที่แสดงบนหน้าจอจริง (เคยพบรหัสผ่านตัวแทนติดมาด้วย) จึงไม่ควรอยู่ใน git
.playwright-mcp/

# ไฟล์ขยะจาก hook แจ้งเตือน (พาธ Windows ถูกเขียนเป็นชื่อไฟล์ตรง ๆ) — ห้ามเข้า repo
*claude-notify-signals*

# ข้อมูลสำรอง — มีข้อมูลลูกค้าจริงและรหัสลับตัวแทน ห้ามขึ้น git เด็ดขาด
backups/
.supabase-test-db-password.local

# ── Vercel ────────────────────────────────────────────────────────
# .vercel/ เก็บรหัสโปรเจกต์ที่ผูกไว้กับเครื่องนี้ + โทเค็นชั่วคราว — เป็นของเฉพาะเครื่อง
.vercel


# สื่อประกอบการนำเสนอ (ภาพหน้าจอ/วิดีโอ) — ไฟล์ใหญ่ ไม่ต้องเก็บใน git
สื่อนำเสนอ/
