# รายการตรวจสอบก่อนเปิดใช้จริง

ปรับปรุงล่าสุด 14 ส.ค. 2569 · ทุกข้อต้องผ่านก่อนให้ตัวแทนรายแรกเข้าใช้งาน

สถานะ: ⬜ ยังไม่ทำ · ✅ ทำแล้ว · ⛔ ติดอยู่ รอคนอื่นตัดสินใจ

> **สรุปสถานะ 14 ส.ค. 69** — ฝั่งระบบพร้อมครบแล้ว: `npm run preflight` ผ่านทุกข้อบนฐานจริง ·
> โครงสร้างฐานข้อมูลอัปเดตครบถึงใบ 0140 · ชุดทดสอบเต็ม 268 ข้อผ่าน ไม่มีข้อล้มและไม่มีข้อวูบ
> **ที่เหลือทั้งหมดคือ "ข้อมูลของธุรกิจ" ที่ต้องมาจากทางเบนจามิน** — รวบรวมเป็นใบเดียวไว้ที่
> [ข้อมูลที่ต้องกรอกก่อนเปิดใช้จริง.md](./ข้อมูลที่ต้องกรอกก่อนเปิดใช้จริง.md) (ส่งต่อให้ผู้เกี่ยวข้องได้เลย)
>
> เร่งด่วนที่สุด: **ราคากลางแม่แบบยังเป็น ฿0 ทุกตัว → ตัวแทนออกใบเสนอราคาไม่ได้เลยสักใบ**

---

## 1. แยกฐานข้อมูลทดสอบออกจากฐานข้อมูลจริง ✅ (11 ส.ค. 69)

ตอนนี้มีฐานข้อมูล **2 ชุด** แยกขาดจากกัน

| ชุด | โปรเจกต์ | ใครใช้ |
|---|---|---|
| ชุดจริง | `pms` · `yhhhcrvhkforwsagojho` | ระบบที่จะเปิดใช้จริง · สคริปต์สำรอง/กู้คืน/ตรวจความพร้อม |
| ชุดทดสอบ | `pms-test` · `fxckixlzsfqalqukhuud` | ตอนพัฒนาในเครื่อง · ชุดทดสอบอัตโนมัติ · ทดสอบโหลด |

**ค่าเชื่อมต่ออยู่ที่ไหน**
- `apps/hq/.env.local` · `apps/dealer/.env.local` → **ชุดทดสอบ**
- `.env.production.local` → **ชุดจริง** (สคริปต์ดูแลระบบอ่านจากที่นี่ผ่าน `scripts/lib/targetEnv.mjs`)
- ทุกสคริปต์บอกก่อนทำงานว่ากำลังแตะฐานไหน — อ่านบรรทัด `🎯 ฐานข้อมูลที่จะแตะ:` ก่อนกดทุกครั้ง

> ⚠️ **ห้ามให้สคริปต์ดูแลระบบอ่าน `apps/hq/.env.local`** — ไฟล์นั้นชี้ชุดทดสอบ
> ถ้าพลาด การสำรองข้อมูลจะไปสำรองชุดทดสอบแทนชุดจริงโดยไม่มีอะไรเตือน แล้ววันที่ต้องกู้จริงถึงจะรู้ว่าไฟล์สำรองทั้งกองไม่มีข้อมูลลูกค้าเลย

**เวลาสร้างฐานข้อมูลใหม่ ต้องทำเพิ่ม 1 อย่างที่ไมเกรชันทำให้ไม่ได้**

`supabase config push --project-ref <รหัสโปรเจกต์>` — เปิด hook ที่ยัดสิทธิ์ลงใบผ่านเข้าระบบ
ไม่ทำ = ทุกคนล็อกอินได้แต่เห็นข้อมูลว่างเปล่าทุกหน้า (เจอตอนแยกฐาน ชุดทดสอบตก 45 ข้อ)
ตอนนี้ค่านี้เขียนไว้ใน `supabase/config.toml` แล้ว ไม่ต้องพึ่งความจำใคร

---

## 2. ล้างร่องรอยการทดสอบออกจากฐานข้อมูลจริง ✅ (11 ส.ค. 69)

ทำด้วย `node scripts/go-live-reset.mjs --apply --yes --dealers=...`

- ✅ สำรองข้อมูลก่อน — `backups/2026-08-110417` (11,037 แถว)
- ✅ ลบตัวแทนสมมติ 3 ราย (RYG · CNX · UBN) พร้อมบัญชีล็อกอิน
- ✅ ล้างบันทึกการใช้งาน 10,972 แถว
- ✅ รีเซ็ตตัวนับเลขที่เอกสารให้เริ่มที่ 1 ทุกสาขา
- ✅ ยืนยันว่าตารางงานขายว่างจริงทั้งหมด

**สภาพฐานจริงตอนนี้** — ตัวแทน 0 ราย · บัญชี 1 (ผู้ดูแลสำนักงานใหญ่ ล็อกอินได้) · งานขาย 0 · แม่แบบสินค้า 6 · ข้อมูลบริษัทกรอกแล้ว 3 ช่อง

---

## 3. ข้อมูลตั้งต้นที่ต้องมีก่อนเปิดใช้

### 3.1 ข้อมูลบริษัทเบนจามิน (หน้าตั้งค่า › บริษัท)

ข้อมูลนี้ไปโผล่บนใบเสนอราคาที่ตัวแทนส่งให้ลูกค้า กรอกผิด = เอกสารที่ส่งออกไปแล้วผิดตาม

- ✅ ชื่อบริษัท · ที่อยู่ · โทรศัพท์ — ดึงมาจากระบบเดิม (`C:\.claude\file-benjamintomail`)
- ⛔ **เลขประจำตัวผู้เสียภาษี** — ระบบเดิมไม่เคยกรอก ต้องขอจากฝ่ายบัญชี
- ⛔ **อีเมลกลาง** — ระบบเดิมเป็นค่าตัวอย่างที่ติดมากับตัวติดตั้ง (`info@example.com`) ใช้ไม่ได้
- ⛔ **เว็บไซต์** — ระบบเดิมไม่มีช่องนี้
- ⬜ ตรวจว่าชื่อที่กรอกตรงกับชื่อจดทะเบียนจริง (ระบบเดิมเขียนไว้ว่า "เบญจมินทร์ ( BENJAMIN )" ซึ่งไม่ใช่รูปแบบชื่อนิติบุคคล)

### 3.2 ทะเบียนตัวแทนจำหน่าย ⛔

- ⛔ รายชื่อตัวแทนจริง + จังหวัด + ภูมิภาค (ฐานจริงตอนนี้มีรายเดียวคือ DSF ที่ใช้ทดสอบ — ตรวจ 3 ก.ย. 69)
- ⛔ เป้ายอดขายรายปีของแต่ละราย
- ⬜ สร้างบัญชีล็อกอินให้แต่ละราย แล้วส่งรหัสผ่านให้ทางช่องทางที่ปลอดภัย
- ⬜ ให้แต่ละสาขาเข้าไปตั้งค่าเอกสารของตัวเอง (คำนำหน้าเลขที่ใบ · VAT · ข้อความท้ายเอกสาร)

> หมายเหตุ: ระบบออกแบบให้ **1 ตัวแทน = 1 บัญชี** โดยตั้งใจ ไม่ใช่ข้อจำกัดที่ลืมทำ

### 3.3 แคตตาล็อกสินค้าและราคากลาง ⛔ ← เร่งด่วนที่สุด

- ⛔ **ฐานข้อมูลจริงยังไม่มีแม่แบบสักรายการ (0 แถว)** → ตัวแทนออกใบเสนอราคาไม่ได้เลยสักใบ (ตรวจ 3 ก.ย. 69)
  วิธีที่เร็วที่สุด: หน้าตั้งค่า › บริษัท › **ส่งออก (สำรองข้อมูล)** จะได้ไฟล์ Excel ที่มีหัวตารางครบทุกแท็บ
  พิมพ์แม่แบบ/ราคา/รายชื่อตัวแทนลงไป แล้ว **นำเข้า (กู้คืน)** กลับ — เข้าทีเดียวทั้งชุด
- ⛔ แม่แบบย่อยรวม 23 แบบ ยังไม่มีการตั้งราคาแยกรายตัวสักแบบ (ระบบรองรับแล้ว — ไม่ตั้ง = ใช้ราคาแม่แบบหลัก)
- ⬜ ตรวจว่าแม่แบบย่อยของแต่ละแม่แบบตรงกับที่ขายจริง
- ⬜ ตั้งวันที่เริ่มใช้ราคาให้ถูกต้อง

> ⚠️ ราคากลางเป็นตัวตั้งของทุกใบเสนอราคา ผิดที่นี่ = ผิดทั้งเครือ

### 3.4 นโยบายและเป้าหมายของสำนักงานใหญ่ ⛔

- ⛔ เป้ายอดขายรวมทั้งปี — ตอนนี้ตั้งไว้ 260 ล้าน แต่เป้าของตัวแทนรวมกันได้ 55.5 ล้าน ไม่ตรงกัน
- ⬜ อัตราปิดการขายเป้าหมาย · VAT · อายุใบเสนอราคา
- ⬜ เหตุผลที่ปิดการขายไม่สำเร็จ — ตรวจว่าครอบคลุมของจริง

---

## 4. ความปลอดภัยของข้อมูล

- ✅ **ตั้งเวลาสำรองข้อมูลอัตโนมัติ** (13 ส.ค. 69) — งานตามเวลาของ Windows ทุกวัน 02:00 น. · ดู [BACKUP.md](./BACKUP.md#สำรองอัตโนมัติรายวัน--ตั้งแล้ว-13-สค-69)
- ⬜ **เก็บสำเนาไว้นอกเครื่องนี้** ← ค้างอยู่ — เครื่องมือพร้อมแล้ว (14 ส.ค. 69) เหลือตั้งค่าครั้งเดียว:
  `setx BACKUP_DIR "C:\Users\boomb\OneDrive\benjamin-backups"` แล้วสำรองสักครั้งเพื่อยืนยัน (ดู docs/BACKUP.md)
  ทางเลือกที่เสนอไว้: เปิดบริการสำรองของ Supabase (ย้อนเวลาได้ทุกจุด + กู้รหัสผ่านผู้ใช้ได้ · มีค่าใช้จ่ายรายเดือน)
  — ไม่แนะนำให้ส่งไฟล์สำรองไปเก็บบน GitHub เพราะมีข้อมูลลูกค้าจริงและรหัสตัวแทน
- ✅ **ทดลองกู้คืนจริง** (13 ส.ค. 69) — `npm run backup:drill` ผ่านครบทุกขั้น
  จำลองลบข้อมูลผิดแล้วกู้กลับ · ข้อมูลตรงครบ 28 ช่อง · หลังกู้ยังบันทึกรายการใหม่ได้โดยเลขไม่ชนของเดิม
- ⬜ เปลี่ยนรหัสผ่านบัญชีผู้ดูแลระบบ ไม่ให้ซ้ำกับที่ใช้ตอนพัฒนา
- ⬜ ตรวจว่าโฟลเดอร์ `backups/` ยังอยู่ใน `.gitignore` (มีข้อมูลลูกค้าจริงและรหัสลับตัวแทน)

---

## 5. ขึ้นระบบจริง

- ✅ นำระบบขึ้นใช้งานแล้ว (Vercel) — ฝั่งตัวแทน `benjamin-dealer.vercel.app` · ฝั่งสำนักงานใหญ่แยกอีกที่อยู่
- ⬜ ชื่อเว็บของตัวเอง (โดเมน) — ถ้าต้องการ ไม่ใช่ตัวขวางการเปิดใช้
- ✅ ระบบแจ้งเตือนเวลาระบบพัง — เปิดใช้แล้ว (Sentry) · ⬜ ยังไม่ได้ทดลองให้พังดูจริง 1 ครั้ง
- ✅ **`npm run preflight` ผ่านครบทุกข้อบนฐานจริง** (14 ส.ค. 69)
- ✅ **โครงสร้างฐานข้อมูลอัปเดตครบถึงใบ 0168** (3 ก.ย. 69 · ตรวจเทียบ local/remote แล้ว ไม่ขาดใบไหน)
- ✅ **อีเมลบริษัท (SMTP) เปิดใช้แล้ว** (3 ก.ย. 69) — ส่งผ่าน smtp.gmail.com ในชื่อผู้ส่ง "Benjamin PMS" · ใช้กับลืมรหัสผ่านและดูรหัสของตัวเอง
- ✅ **ขึ้นระบบครบ 4 เว็บ** (3 ก.ย. 69) — สำนักงานใหญ่ · ตัวแทน · demo สองตัว รุ่นเดียวกันหมด
- ⬜ รันชุดทดสอบเต็มบนระบบจริงหนึ่งรอบก่อนเปิดให้คนใช้
  (ชุดทดสอบเต็ม **415 ข้อผ่าน ไม่มีข้อล้ม** บนฐานทดสอบแล้ว · 3 ก.ย. 69 · ยิงใส่ฐานจริงจะทิ้งข้อมูลทดสอบชั่วคราว ต้องขออนุญาตก่อน)

---

## 6. ก่อนส่งมอบให้ผู้ใช้ ⬜

- ⬜ ทดลองใช้จริงกับตัวแทน 1–2 ราย ตั้งแต่สร้างลูกค้าเป้าหมายจนปิดการขาย
- ✅ คู่มือใช้งาน — แยกฉบับตัวแทนกับฉบับสำนักงานใหญ่ (อัปให้ตรงระบบล่าสุด 3 ก.ย. 69)
- ⬜ อบรมผู้ใช้ + ตกลงช่องทางแจ้งปัญหา
- ⬜ ตกลงว่าใครเป็นคนดูแลระบบหลังส่งมอบ

---

## คำสั่งที่ใช้บ่อย

| ทำอะไร | คำสั่ง |
|---|---|
| สำรองข้อมูล | `npm run backup` |
| ทดลองกู้คืน (ไม่แตะของจริง) | `npm run backup:drill` |
| ตรวจความพร้อมก่อนขึ้นระบบ | `npm run preflight` |
| ชุดทดสอบเต็ม | `npm run test` |
| ตัวตรวจกับดักที่เคยพลาดมาแล้ว | `npm run checks` |
